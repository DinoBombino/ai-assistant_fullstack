{
  "scripts": {
    "start": "cd server && npm run start",
    "build": "cd client && npm install && npm run build && cd ../server && npm run build"
  },
  "dependencies": {
    "typescript": "~5.2.0",
    "vite": "^7.1.10"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  },
  "engines": {
    "node": ">=18.20.8"
  }
}
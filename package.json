{
  "scripts": {
    "start": "cd server && npm run start",
    "build": "cd client && npm install && npm run build && cd ../server && npm run build"
  },
  "dependencies": {
    "vite": "^5.4.20"
  },
  "engines": {
    "node": ">=18"
  }
}